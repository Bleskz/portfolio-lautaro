# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.vercel

# Claude Code — no subir al repo
CLAUDE.md
CONTEXT.md
AUDIT.md
recordatorios.md
.claude/

# CV sources — only the rendered PDFs in /public are committed (served by Vercel).
# The /cv working folder (drafts, instructions, vocab guides) stays local.
cv/
